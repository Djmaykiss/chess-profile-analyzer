# Testing

Antes de cerrar una fase: `npm run lint`, `npm run typecheck`, `npm run test` y `npm run build`. Se añadirán pruebas unitarias del parser, resultados, ECO y estadísticas; integración de importadores y duplicados; y pruebas RLS entre usuarios.
