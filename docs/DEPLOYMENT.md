# Deployment

El frontend se desplegará en Netlify y el backend en Supabase. Antes de producción se añadirán manifest, iconos y service worker para PWA, variables de entorno seguras y auditoría de Lighthouse.
