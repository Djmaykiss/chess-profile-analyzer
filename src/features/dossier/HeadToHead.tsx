import { ProfileComparison } from './dossier.types'
export function HeadToHead({ data }: { data: ProfileComparison['head_to_head'] }) { return <section className="card"><p className="eyebrow">ENFRENTAMIENTOS DIRECTOS</p><h2>Head-to-head</h2>{data.games ? <><p className="head-to-head-score">{data.left_wins} — {data.draws} — {data.right_wins}</p><p className="subtle-copy">{data.games} partidas: victorias izquierda · tablas · victorias derecha.</p><ul className="head-to-head-list">{data.recent_games.map(game => <li key={`${game.game_url}-${game.played_at}`}><span>{new Date(game.played_at).toLocaleDateString()} · {game.white_username} vs {game.black_username}</span>{game.game_url && <a href={game.game_url} target="_blank" rel="noreferrer">Abrir partida</a>}</li>)}</ul></> : <p className="subtle-copy">No hay enfrentamientos directos importados entre los aliases de estos perfiles.</p>}</section> }
